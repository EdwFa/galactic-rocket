# 🔍 RAGFlow Semantic Search

Streamlit приложение для семантического поиска чанков через RAGFlow API.

![RAGFlow Integration](https://img.shields.io/badge/RAGFlow-Integration-blue)
![Streamlit](https://img.shields.io/badge/Streamlit-1.28+-red)
![Python](https://img.shields.io/badge/Python-3.10+-green)

## ✨ Возможности

- 🔌 **Подключение к RAGFlow** — настройка URL и API ключа
- 📚 **Выбор датасетов** — поиск по одному или нескольким датасетам
- 🔍 **Семантический поиск** — получение релевантных чанков по запросу
- 🎛️ **Настройка параметров** — порог схожести, вес векторов, количество результатов
- ✨ **Подсветка терминов** — выделение найденных слов в результатах
- 📊 **Статистика** — средняя схожесть, количество документов

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка RAGFlow

Убедитесь, что у вас:

- Запущен RAGFlow сервер (локально или в облаке)
- Получен API ключ в настройках RAGFlow
- Создан хотя бы один датасет с документами

### 3. Запуск приложения

```bash
streamlit run app.py
```

### 4. Использование

1. Введите URL RAGFlow сервера (например, `http://localhost:9380`)
2. Введите ваш API ключ
3. Нажмите "Подключиться"
4. Выберите датасеты для поиска
5. Введите запрос и нажмите "Искать"

## 📁 Структура проекта

```
galactic-rocket/
├── app.py              # Streamlit приложение
├── ragflow_client.py   # Клиент для RAGFlow API
├── requirements.txt    # Зависимости
├── .env.example        # Пример конфигурации
└── README.md           # Документация
```

## 🔧 API Reference

### RAGFlowClient

```python
from ragflow_client import RAGFlowClient

# Создание клиента
client = RAGFlowClient(
    base_url="http://localhost:9380",
    api_key="your-api-key"
)

# Получение списка датасетов
datasets = client.list_datasets()

# Поиск чанков
chunks = client.search(
    question="Как работает RAG?",
    dataset_ids=["dataset-id-1"],
    top_k=5,
    similarity_threshold=0.3
)

# Обработка результатов
for chunk in chunks:
    print(f"[{chunk.similarity:.1%}] {chunk.content[:100]}...")
```

### Параметры поиска

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `question` | str | — | Поисковый запрос |
| `dataset_ids` | list | — | ID датасетов |
| `top_k` | int | 10 | Количество результатов |
| `similarity_threshold` | float | 0.2 | Минимальный порог схожести |
| `vector_similarity_weight` | float | 0.3 | Вес векторного сходства |
| `highlight` | bool | True | Подсветка терминов |
| `keyword` | bool | False | Поиск по ключевым словам |

## 🐳 RAGFlow через Docker

Если у вас ещё нет RAGFlow:

```bash
# Клонирование репозитория
git clone https://github.com/infiniflow/ragflow.git
cd ragflow

# Запуск через Docker
docker compose up -d
```

После запуска RAGFlow будет доступен по адресу `http://localhost:9380`

## 📝 Лицензия

MIT License
